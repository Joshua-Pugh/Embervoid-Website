const canvas=document.querySelector('#embers');
const reduceMotion=matchMedia('(prefers-reduced-motion: reduce)').matches;
if(canvas){const ctx=canvas.getContext('2d');let embers=[];function resize(){canvas.width=innerWidth*devicePixelRatio;canvas.height=innerHeight*devicePixelRatio;canvas.style.width=`${innerWidth}px`;canvas.style.height=`${innerHeight}px`;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);embers=Array.from({length:reduceMotion?18:Math.min(70,Math.floor(innerWidth/20))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.4+.35,s:Math.random()*.28+.08,d:(Math.random()-.5)*.18,a:Math.random()*.55+.12}))}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);embers.forEach(e=>{const g=ctx.createRadialGradient(e.x,e.y,0,e.x,e.y,e.r*5);g.addColorStop(0,`rgba(255,180,90,${e.a})`);g.addColorStop(.25,`rgba(237,79,35,${e.a*.65})`);g.addColorStop(1,'rgba(100,20,5,0)');ctx.fillStyle=g;ctx.beginPath();ctx.arc(e.x,e.y,e.r*5,0,Math.PI*2);ctx.fill();if(!reduceMotion){e.y-=e.s;e.x+=e.d;if(e.y< -10){e.y=innerHeight+10;e.x=Math.random()*innerWidth}}});requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw()}
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>entry.target.classList.toggle('visible',entry.isIntersecting)),{threshold:.15});document.querySelectorAll('.reveal').forEach(element=>observer.observe(element));
const year=document.querySelector('#year');if(year)year.textContent=new Date().getFullYear();
const header=document.querySelector('.site-header');const nav=header?.querySelector('nav');if(header&&nav){const button=document.createElement('button');button.className='home-menu';button.type='button';button.setAttribute('aria-expanded','false');button.textContent='Explore';header.appendChild(button);button.addEventListener('click',()=>{const open=header.classList.toggle('menu-open');button.setAttribute('aria-expanded',String(open));button.textContent=open?'Close':'Explore'});nav.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>header.classList.remove('menu-open')))}
